"""Booking engine URLs.

Public-site routes are intentionally not scaffolded in the default booking core.
Projects that need a website booking UI should use a dedicated public layer.
"""

app_name = "booking"

urlpatterns = []
