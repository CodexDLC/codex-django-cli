# codex-django-cli

`codex-django-cli` is the standalone scaffolding layer extracted from `codex-django`.

It owns the interactive command flow, blueprint rendering, scaffold commands, CLI templates, and CLI-focused tests. Generated projects still target the `codex_django.*` runtime API from `codex-django`.
